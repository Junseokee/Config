server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Junseokee/Spring-Cloud-Config
          search-paths: config/**
          default-label: main